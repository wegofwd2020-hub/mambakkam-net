dist
node_modules
.github
.changeset

# Dropped-in product builds (e.g. Mentible Expo-web export) are bundled,
# minified artifacts — never hand-format them. See docs/DEMO_mentible_lite.md.
public/demos
public/app

# Atri Sangam replay data, exported by `atri-sangam-export-demo --json`. Written
# compact on purpose: pretty-printing the sample arrays roughly triples the
# files, and they are regenerated wholesale rather than edited, so a readable
# diff buys nothing. See docs/DESIGN_atri_sangam_replays.md.
src/data/atri-sangam

# Playwright MCP snapshots — tool output, not project content.
.playwright-mcp
